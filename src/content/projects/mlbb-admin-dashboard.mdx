---
title: 'MLBB Admin Dashboard'
slug: 'mlbb-admin-dashboard'
year: '2025'
role: 'Frontend Developer'
description: 'Web admin panel berbasis Vue.js untuk mengelola data API Mobile Legends. Dashboard terintegrasi dengan backend GraphQL untuk operasi CRUD hero, item, build, dan konfigurasi meta game.'
descriptionEn: 'A Vue.js-based admin web panel for managing Mobile Legends API data. Integrated with the GraphQL backend for CRUD operations on heroes, items, builds, and meta game configurations.'
techStack:
  - 'Vue.js'
  - 'GraphQL'
  - 'Tailwind CSS'
  - 'TypeScript'
  - 'Apollo Client'
accent: 'primary'
duration: '2 bulan'
teamSize: 'Solo'
status: 'live'
coverImage: '../../assets/images/web-admin-mlbb/data-hero-page.jpg'
screenshots:
  - src: '../../assets/images/web-admin-mlbb/data-hero-page.jpg'
    alt: 'Data Hero Page'
  - src: '../../assets/images/web-admin-mlbb/create-hero-modal.jpg'
    alt: 'Create Hero Modal'
  - src: '../../assets/images/web-admin-mlbb/draft-pick-simulation.jpg'
    alt: 'Draft Pick Simulation'
  - src: '../../assets/images/web-admin-mlbb/navigation-menu.jpg'
    alt: 'Navigation Menu'
  - src: '../../assets/images/web-admin-mlbb/tournament page.png'
    alt: 'Tournament Page'
---

<div data-lang="id">

## Overview

**MLBB Admin Dashboard** adalah companion frontend untuk MLBB Data APIs. Dibangun dengan Vue.js + Apollo Client, dashboard ini memberikan UI yang clean dan efisien untuk admin mengelola data game lewat operasi CRUD yang terintegrasi dengan GraphQL backend.

## Masalah

Backend GraphQL membutuhkan UI yang bisa:

- Manage data hero, item, build, dan patch dengan **mudah**
- Memberikan feedback **real-time** saat data berubah
- Handle bulk operations untuk update massal (misal saat patch baru)
- **Type-safe** end-to-end dari schema sampai UI

## Solusi

Vue.js dipilih karena reactivity-nya yang clean dan ekosistemnya yang mendukung type-safety dengan TypeScript.

### Fitur Utama

- **Hero Management** — CRUD interface dengan drag-and-drop ordering
- **Item & Build Editor** — editor visual dengan preview
- **Patch Manager** — bulk update meta stats per patch dengan diff view
- **Live GraphQL Playground** — embedded GraphiQL untuk testing query
- **Optimistic UI** — instant feedback untuk user, sync dengan server di background

### Apollo Client Setup

```typescript
import { ApolloClient, InMemoryCache, createHttpLink } from '@apollo/client/core';
import { setContext } from '@apollo/client/link/context';

const httpLink = createHttpLink({
  uri: import.meta.env.VITE_GRAPHQL_ENDPOINT,
});

const authLink = setContext((_, { headers }) => {
  const token = localStorage.getItem('mlbb_admin_token');
  return {
    headers: {
      ...headers,
      authorization: token ? `Bearer ${token}` : '',
    },
  };
});

export const apolloClient = new ApolloClient({
  link: authLink.concat(httpLink),
  cache: new InMemoryCache({
    typePolicies: {
      Hero: {
        fields: {
          recommendedBuilds: {
            merge(_, incoming) { return incoming; },
          },
        },
      },
    },
  }),
});
```

### Composable Pattern (Vue 3)

```typescript
export function useHeroes() {
  const { result, loading, refetch } = useQuery(HEROES_QUERY);

  const heroes = computed(() => result.value?.heroes ?? []);
  const heroesByRole = computed(() => groupBy(heroes.value, 'role'));

  return { heroes, heroesByRole, loading, refetch };
}
```

## Tech Stack

| Layer | Teknologi |
|---|---|
| Framework | Vue.js 3 dengan Composition API |
| State / Data | Apollo Client untuk GraphQL state management |
| Styling | Tailwind CSS dengan custom design tokens |
| Forms | VeeValidate + Zod schema |
| Routing | Vue Router dengan route guards |
| Build | Vite dengan TypeScript strict mode |

## Tantangan & Solusi

**Tantangan 1: Cache consistency setelah mutation**
Solusi: Menggunakan Apollo Client `update` callback untuk update cache manual setelah mutation, plus refetch queries strategis untuk data yang complex.

**Tantangan 2: Bulk operation UX**
Solusi: Implementasi optimistic updates dengan rollback jika server error, plus progress indicator untuk operasi panjang.

**Tantangan 3: Form validation yang complex**
Solusi: VeeValidate + Zod schema yang **shared** dengan backend. Schema validation jadi single source of truth.

## Integrasi dengan Backend

Dashboard ini **tightly integrated** dengan [MLBB Data APIs](/projects/mlbb-data-apis) sebagai consumer utama. Setiap perubahan di admin dashboard langsung tersimpan via GraphQL mutations dan ter-reflect di public API.

## Pelajaran yang Didapat

- **Composition API** sangat clean untuk extracting logic yang reusable
- **Apollo cache** adalah double-edged sword — powerful tapi butuh attention untuk consistency
- **Type-safe end-to-end** (GraphQL Codegen → TypeScript types → Vue components) sangat worth it untuk DX
- Optimistic UI butuh perencanaan careful untuk error handling

</div>

<div data-lang="en">

## Overview

**MLBB Admin Dashboard** is the companion frontend for MLBB Data APIs. Built with Vue.js + Apollo Client, this dashboard provides a clean and efficient UI for admins to manage game data through CRUD operations integrated with the GraphQL backend.

## The Problem

The GraphQL backend needed a UI that could:

- Manage hero, item, build, and patch data **easily**
- Provide **real-time** feedback when data changes
- Handle bulk operations for mass updates (e.g., when a new patch drops)
- Be **type-safe** end-to-end from schema to UI

## The Solution

Vue.js was chosen for its clean reactivity and ecosystem support for type-safety with TypeScript.

### Key Features

- **Hero Management** — CRUD interface with drag-and-drop ordering
- **Item & Build Editor** — visual editor with preview
- **Patch Manager** — bulk update meta stats per patch with diff view
- **Live GraphQL Playground** — embedded GraphiQL for testing queries
- **Optimistic UI** — instant feedback for users, syncs with server in background

### Apollo Client Setup

```typescript
import { ApolloClient, InMemoryCache, createHttpLink } from '@apollo/client/core';
import { setContext } from '@apollo/client/link/context';

const httpLink = createHttpLink({
  uri: import.meta.env.VITE_GRAPHQL_ENDPOINT,
});

const authLink = setContext((_, { headers }) => {
  const token = localStorage.getItem('mlbb_admin_token');
  return {
    headers: {
      ...headers,
      authorization: token ? `Bearer ${token}` : '',
    },
  };
});

export const apolloClient = new ApolloClient({
  link: authLink.concat(httpLink),
  cache: new InMemoryCache({
    typePolicies: {
      Hero: {
        fields: {
          recommendedBuilds: {
            merge(_, incoming) { return incoming; },
          },
        },
      },
    },
  }),
});
```

### Composable Pattern (Vue 3)

```typescript
export function useHeroes() {
  const { result, loading, refetch } = useQuery(HEROES_QUERY);

  const heroes = computed(() => result.value?.heroes ?? []);
  const heroesByRole = computed(() => groupBy(heroes.value, 'role'));

  return { heroes, heroesByRole, loading, refetch };
}
```

## Technical Stack

| Layer | Technology |
|---|---|
| Framework | Vue.js 3 with Composition API |
| State / Data | Apollo Client for GraphQL state management |
| Styling | Tailwind CSS with custom design tokens |
| Forms | VeeValidate + Zod schema |
| Routing | Vue Router with route guards |
| Build | Vite with TypeScript strict mode |

## Challenges & Solutions

**Challenge 1: Cache consistency after mutation**
Solution: Using Apollo Client `update` callback for manual cache updates after mutations, plus strategic refetch queries for complex data.

**Challenge 2: Bulk operation UX**
Solution: Implementing optimistic updates with rollback on server error, plus progress indicators for long operations.

**Challenge 3: Complex form validation**
Solution: VeeValidate + Zod schema **shared** with the backend. Schema validation becomes the single source of truth.

## Integration with Backend

This dashboard is **tightly integrated** with [MLBB Data APIs](/projects/mlbb-data-apis) as the primary consumer. Every change in the admin dashboard is saved directly via GraphQL mutations and reflected in the public API.

## Lessons Learned

- **Composition API** is very clean for extracting reusable logic
- **Apollo cache** is a double-edged sword — powerful but requires attention for consistency
- **Type-safe end-to-end** (GraphQL Codegen → TypeScript types → Vue components) is absolutely worth it for DX
- Optimistic UI requires careful planning for error handling

</div>
